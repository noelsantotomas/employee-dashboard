import { StyledHighlightContainer } from "./styles";

const HighlightContainer = ({ children }) => {
  return <StyledHighlightContainer>{children}</StyledHighlightContainer>;
};

export default HighlightContainer;
